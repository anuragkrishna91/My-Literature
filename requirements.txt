requests>=2.28
# Optional: only needed for the authenticated (paywall) browser-session path.
# Install with: pip install playwright && python -m playwright install chromium
playwright>=1.40; extra == "auth"
